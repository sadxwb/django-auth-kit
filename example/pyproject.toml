[project]
name = "example"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "django-auth-kit",
]

[tool.uv.sources]
django-auth-kit = { workspace = true }
